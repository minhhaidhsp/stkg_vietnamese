# Nhật ký quyết định tự hành (autonomous mode)

> Mỗi dòng: vấn đề → chọn gì → vì sao. Chỉ ghi quyết định kỹ thuật có nhiều
> phương án hợp lý (không phải quyết định ảnh hưởng số liệu thực nghiệm/kiến
> trúc mâu thuẫn bản thảo — những cái đó vẫn dừng hỏi theo 4 ranh giới).

- 2026-07-03: x̄ (896) vs g(f) (512) lệch chiều ở CT(6) → thêm `retrieval_query_projection` (896→512, học được), giữ g(f)/STE/RE ở 512 làm không gian trung tâm — đã anh xác nhận trực tiếp trước khi vào chế độ tự hành.
- 2026-07-03: u (896, CT8) vs STE(e) (512) cùng lỗi lệch chiều → thêm `ranking_projection` (896→512, TÁCH RIÊNG với retrieval_query_projection) — đã anh xác nhận trực tiếp.
- 2026-07-03: `EntityRankingHead.output_projection` (Bước 3) đổi tên thành `ranking_projection` để khớp tên field mới trong config.yaml, đọc in/out dim trực tiếp từ config thay vì suy luận ngầm — không đổi hành vi, chỉ đổi tên + validate rõ ràng hơn.
- 2026-07-03: `SubgraphRetriever` chuyển từ plain class sang `nn.Module` (giữ tham số `retrieval_query_projection`) — API `relevance()` đổi tên tham số đầu từ `x_bar` thành `x_bar_raw` (896, X thô) để rõ nó tự chiếu nội bộ, không nhận x_bar đã chiếu sẵn từ ngoài.
- 2026-07-03 (chế độ tự hành): venv Python 3.11 riêng cho môi trường huấn luyện (`.venv/`), tách biệt hoàn toàn môi trường mặc định (Python 3.14, dùng cho step1-3 data pipeline) — lý do: transformers==4.42.3 cần tokenizers~=0.19, không có wheel cho Python 3.13/3.14.
- 2026-07-03: stub `flash_attn` rỗng (package giả) để vượt qua `check_imports()` tĩnh của transformers khi tải Vintern — code thật của model đã tự try/except fallback naive attention khi thiếu `flash_attn.flash_attn_interface`, xác nhận qua log "FlashAttention is not installed" khi chạy thật.
- 2026-07-03: `peft==0.12.0` (không phải bản mới nhất) vì peft>=0.13 import `EncoderDecoderCache` từ transformers, chưa tồn tại ở transformers==4.42.3 — xác nhận cùng thời điểm phát hành (giữa 2024), 51 pytest pass sau khi hạ cấp.
- 2026-07-03: Enc_LM(Q) (CT3) = embedding lookup (`embed_tokens`) của Qwen2, KHÔNG chạy qua toàn bộ stack decoder trước W_q — vì Hình 1 tách riêng box "Enc_LM" khỏi box "các lớp còn lại của LLM", và cùng 1 Qwen2ForCausalLM được tái sử dụng ở cả 2 điểm (embedding cho E_Q, full stack cho "remaining layers" xử lý X sau cộng dư).
- 2026-07-03: Lấy k_proj/v_proj/o_proj của LỚP 0 (layer index 0) trong Qwen2 làm "K,V có sẵn của backbone" cho CT(7) — manuscript không chỉ định lớp cụ thể; lựa chọn lớp đầu tiên là mặc định đơn giản nhất, có thể cần ablation lại lớp nào tối ưu sau này (không phải quyết định cuối cùng, ghi rõ để dễ soát lại).
- 2026-07-03: R_final (pooled, 896) cộng dư (broadcast add) vào MỌI vị trí token của X, không chỉ 1 token/CLS — vì manuscript không chỉ định vị trí cụ thể, broadcast-add toàn cục là lựa chọn đơn giản, đối xứng, dễ ablate sau.
- 2026-07-03: null_image_embedding dùng n_null_patches=16 (nhỏ, phục vụ smoke test) thay vì khớp đúng số patch thật của InternViT (1024 patch ở ảnh 448x448) — vì smoke test hiện tại KHÔNG dùng ảnh thật (chưa tải ảnh Wikimedia về máy), nên không có nhu cầu trộn ảnh thật+placeholder trong cùng batch. PHẢI sửa lại đúng 1024 (hoặc số patch thật) trước khi có batch trộn ảnh thật.
- 2026-07-03 (auto_checks phát hiện, tự sửa): `wikipedia_heritage_collector.py::_cell_label()` sót cú pháp wiki (`]]`/`[[`) trong 4/1679 facts — 3 ca do regex cũ loại trừ ký tự `#` (anchor mục trong wikilink dạng `[[Trang#Mục|Hiển thị]]`), 1 ca do CHÍNH TRANG WIKIPEDIA NGUỒN thiếu `[[` mở (lỗi cú pháp thật trên Wikipedia, xác nhận qua wikitext thật). Sửa: cho phép `#` trong regex + dọn phòng thủ mọi `[[`/`]]` còn sót sau khi match. Chạy lại toàn bộ pipeline hạ nguồn (heritage collector → combined_raw → enriched → questions → split → manifest → auto_checks).

## Phát hiện cần anh chú ý (không phải bug, là đặc điểm dữ liệu — KHÔNG tự quyết vì ảnh hưởng thiết kế L_QA)
- **Đa đáp án cho 1 câu hỏi (multi-valid-answer)**: 1.438/17.450 câu hỏi (8.2%) trùng lặp text hoàn toàn nhưng khác fact_id — nguyên nhân chính: một sự kiện `occurredAt` có NHIỀU giá trị địa điểm hợp lệ trong Wikidata (P276 đa trị, vd "Chiến tranh Đông Dương lần thứ ba" gắn với 8 địa điểm khác nhau: bán đảo Đông Dương, Việt Nam, Campuchia, Đông Nam Á, Trung Quốc, Thái Lan, Lào). Vì template chỉ che t (không hiện trong câu hỏi), câu hỏi giống hệt nhau nhưng có tới 8 đáp án "đúng" khác nhau trong tập dữ liệu — mâu thuẫn với giả định 1 đáp án đúng của L_QA (cross-entropy trên CT8). Cần anh quyết định trước khi huấn luyện đầy đủ: (a) chỉ giữ 1 địa điểm chính/canonical mỗi sự kiện, (b) coi là multi-label và đổi loss/eval cho phù hợp, hay (c) chấp nhận nhiễu này ở quy mô nhỏ (8.2%). Xem results/dataset/auto_checks.json để biết danh sách đầy đủ.
- 2026-07-03 (smoke test, bug thật đã sửa): `ste_entities_full` (STE của toàn bộ vũ trụ thực thể cho ranking head) bị tính MỘT LẦN trước vòng lặp huấn luyện rồi tái sử dụng qua nhiều step — vì STE có tham số huấn luyện, graph của lần tính trước bị giải phóng sau backward() đầu tiên, step thứ 2 trở đi lỗi "Trying to backward through the graph a second time". Xác nhận nguyên nhân chính xác qua `torch.autograd.set_detect_anomaly(True)`. Sửa: tính lại `ste_entities_full` bên trong vòng lặp, mỗi step một lần.

- 2026-07-04 (xử lý trùng lặp câu hỏi, phương án đã chốt): loại 771/17450 câu hỏi trùng text (giữ bản ghi nguồn ưu tiên cao nhất: Wikidata > Wikipedia timeline > Wikipedia heritage, trong cùng nguồn giữ fact_id nhỏ nhất). KHÔNG loại fact khỏi data/spatial/enriched.csv (đồ thị G giữ nguyên 6980 facts). Loại theo quan hệ: {'bornIn': 598, 'occurredAt': 84, 'locatedIn': 45, 'diedIn': 44}. Còn lại 16679 câu hỏi.
  5 ví dụ cụ thể (câu hỏi + fact_id trùng + nguồn):
    - "A Mít sinh ra ở đâu?" (bornIn): fact_id=[1737, 1920], nguồn=['historical_figures.csv', 'historical_figures.csv']
    - "An Như Sơn sinh ra ở đâu?" (bornIn): fact_id=[3883, 3885], nguồn=['historical_figures.csv', 'historical_figures.csv']
    - "Anh Tuấn sinh ra ở đâu?" (bornIn): fact_id=[1725, 2471], nguồn=['historical_figures.csv', 'historical_figures.csv']
    - "Anh Tú sinh ra ở đâu?" (bornIn): fact_id=[1026, 2800], nguồn=['historical_figures.csv', 'historical_figures.csv']
    - "Bùi Anh Tuấn sinh ra ở đâu?" (bornIn): fact_id=[2295, 2878, 3555], nguồn=['historical_figures.csv', 'historical_figures.csv', 'historical_figures.csv']
- 2026-07-04 (lưu ý sau dedupe, KHÔNG tự sửa xlsx): 25/500 dòng trong `validation_sample_500_for_annotators.xlsx` (đã xuất TRƯỚC bước dedupe) tham chiếu fact_id vừa bị loại khỏi `questions.csv`. KHÔNG xáo/tạo lại file xlsx (có thể anh đã bắt đầu chấm) — về bản chất KHÔNG ảnh hưởng chất lượng đánh giá: dedupe chỉ loại BẢN TRÙNG TEXT, giữ lại 1 bản y hệt text ở fact_id khác trong tập chính thức, nên điểm ngữ pháp/ngữ nghĩa/ràng buộc anh chấm cho 25 dòng này vẫn phản ánh đúng chất lượng của câu hỏi đó (vẫn tồn tại trong tập cuối, chỉ khác fact_id/qid). Không cần chấm lại.
- 2026-07-04 (resume/skip theo yêu cầu): `train_one_seed()` giờ kiểm tra checkpoint+file kết quả TRƯỚC khi tải Vintern — nếu cả hai đã tồn tại cho đúng (seed, tag), bỏ qua huấn luyện hoàn toàn (không tải model, tiết kiệm GPU/thời gian), load lại MRR từ file kết quả. Mỗi seed ghi kết quả NGAY sau khi xong (không chờ hết cả batch seeds trong main()) — quan trọng cho C3 (3 seed, rủi ro Colab ngắt runtime cao nhất). Thêm `train.results_dir` (config.yaml) tách biệt với `checkpoint_dir`, mặc định local nhưng ghi đè thành đường dẫn Drive trên Colab (notebook đã cập nhật `RESULTS_ROOT`) — để không mất MRR mỗi seed/alpha khi runtime bị ngắt giữa chừng. Xác nhận thật: chạy 2 lần liên tiếp trên tập nhỏ (12 mẫu, CPU) — lần 2 bỏ qua đúng, không tải lại model, MRR khớp lần 1.
